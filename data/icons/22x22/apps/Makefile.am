themedir = $(datadir)/icons/hicolor
size = 22x22
context = apps

iconsdir = $(themedir)/$(size)/$(context)

icons_DATA = \
	gnome-mud.png

noinst_DATA = \
	gnome-mud.svg

EXTRA_DIST =		\
	$(icons_DATA)	\
	$(noinst_DATA)
